# API URL (optional, defaults to http://localhost:3001)
VITE_API_URL="http://localhost:3001"

# Development token (optional, for auto-login during development)
# Generate with: cd apps/api && pnpm run mint:token -- player1@test.com
VITE_DEV_TOKEN=""
